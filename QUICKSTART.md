# 🚀 Быстрый старт Finance Tracker

## Запуск приложения

```bash
# 1. Активировать виртуальное окружение
source venv/bin/activate

# 2. Запустить приложение
python3 main.py
```

## Как использовать темы

### Шаг 1: Открыть Settings
- В левом меню нажмите на кнопку **⚙️ Settings**

### Шаг 2: Выбрать тему
- В выпадающем списке выберите одну из 5 тем:
  - **Crypto Wallet** (по умолчанию) - фиолетовая
  - **Ocean Breeze** - бирюзовая
  - **Sunset Glow** - оранжево-розовая
  - **Forest Green** - зеленая
  - **Midnight Blue** - синяя

### Шаг 3: Применить
- Нажмите кнопку **Apply Theme**
- Приложение **мгновенно** изменит цвета - перезапуск НЕ требуется!
- UI полностью обновляется автоматически

### Шаг 4: Проверить
- Перейдите в Dashboard, Add Transaction, History
- Все элементы должны иметь новые цвета
- Закройте и откройте приложение - тема сохранится

## Тестирование

```bash
# Быстрый тест системы тем
python3 test_themes.py

# Проверка запуска приложения
python3 test_app.py
```

## Решение проблем

### Ошибка: `ModuleNotFoundError: No module named 'customtkinter'`
```bash
source venv/bin/activate
pip install -r requirements.txt
```

### Ошибка: `No module named 'tomli_w'`
```bash
source venv/bin/activate
pip install tomli-w
```

### Приложение не меняет цвета (НЕ ДОЛЖНО ПРОИСХОДИТЬ)
Если цвета не меняются сразу после нажатия Apply Theme:
1. Проверьте, что используете актуальную версию кода
2. Убедитесь, что файлы `ui/colors.py` и `main.py` обновлены
3. Запустите тесты: `python3 test_ui_refresh.py`

### Тема не сохраняется
Проверьте, что файл `config/settings.toml` создается автоматически:
```bash
cat config/settings.toml
```

## Структура файлов

```
claude_project/
├── main.py                  # Главное приложение
├── theme_manager.py         # Система тем
├── config/
│   ├── themes.toml          # Определения тем
│   └── settings.toml        # Настройки (автосоздается)
└── ui/
    └── colors.py            # Динамические цвета
```

## Добавление своей темы

Отредактируйте `config/themes.toml` и добавьте новую секцию:

```toml
[theme.my_theme]
name = "My Custom Theme"
description = "My awesome color scheme"

dark_bg = "#123456"
accent_purple = "#ABCDEF"
# ... и остальные цвета
```

Перезапустите приложение - новая тема появится в списке!

---

📚 Полная документация: README.md
🧪 Инструкция по тестированию: TESTING.md
